# RIPE RIS 5-minute UPDATE dumps (raw, NOT in git)

Only the +/-1 day windows around the 4 case-study crossings. ~144 GB.
Download with `scripts/17-Fetch_Case_Updates.py`.
Files: `{collector}/{YYYY.MM}/updates.{YYYYMMDD}.{HHMM}.gz`. Source: https://ris.ripe.net/
