# Case-study update replay inputs (regenerated, NOT in git)

`{case}_anchors.pkl` + `{case}_events.pkl` (~3.9 GB). Produced by `scripts/18-Extract_Case_Updates.py`
from the raw updates; consumed by step 20.
